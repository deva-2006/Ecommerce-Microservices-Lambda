name: Deploy Notification Consumer Service

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'notification-consumer-lambda/**'

permissions:
  id-token: write
  contents: read

jobs:
  build-and-test:
    name: Build & Unit Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: |
          cd notification-consumer-lambda
          mvn clean package -DskipTests

      - name: Unit Tests
        run: |
          cd notification-consumer-lambda
          mvn test

      - name: Save Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: notification-consumer-jar
          path: notification-consumer-lambda/target/notification-consumer-lambda-1.0.0.jar

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install Snyk CLI
        uses: snyk/actions/setup@master

      - name: Run Snyk Security Scan (JSON Output)
        run: |
          snyk test --file=notification-consumer-lambda/pom.xml --severity-threshold=high --json > snyk-report.json || true
        env:
          SNYK_TOKEN: ${{ secrets.DEVASECRETSNYK }}

      - name: Convert JSON to HTML Report
        if: always()
        run: |
          if [ -f snyk-report.json ]; then
            npx snyk-to-html -i snyk-report.json -o snyk-report.html
          fi

      - name: Upload Snyk Security Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: snyk-notification-consumer-report
          path: |
            snyk-report.json
            snyk-report.html

      - name: SonarQube Scan
        run: |
          cd notification-consumer-lambda
          mvn clean test-compile org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.organization=deva-2006 \
            -Dsonar.projectKey=deva-2006_notification-consumer-lambda \
            -Dsonar.branch.name=main \
            -Dsonar.host.url=https://sonarcloud.io
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  deploy:
    name: Deploy to AWS
    needs: [build-and-test, security-scan]
    runs-on: ubuntu-latest
    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: notification-consumer-jar
          path: notification-consumer-lambda/target

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::943755888207:role/GitHubActionsWorkflowRole
          aws-region: us-east-1

      - name: Upload to S3
        run: |
          aws s3 cp notification-consumer-lambda/target/notification-consumer-lambda-1.0.0.jar s3://deva-lambda-artifacts/notification-consumer-lambda.jar

      - name: Deploy to AWS Lambda
        run: |
          aws lambda update-function-code \
            --function-name notification-consumer-lambda \
            --s3-bucket deva-lambda-artifacts \
            --s3-key notification-consumer-lambda.jar